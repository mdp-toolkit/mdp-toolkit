realistic TODO:

MDP 2.0.0 28.10.2005:

- check tests: should run without failures or errors up to 10000 times. (zito)
- test PCANode when output_dim = float (zito)
- document new nodes, check old ones also (berkes)
- decorators for automagically handle docstrings (zito?)
- check iterators generators in flow (only allow generators for single phase nodes)
  (remember : type <generator>) + test (berkes)
- kalman filters (berkes)
- TUTORIAL! (who knows?)
- demos ? 
- site: add citation section, state email addresses, rewrite description to highlight
  education, framework for new algorithms, title outlook, modify mantainers, modify 
  compilation instruction for scipy, add migration faq.
- memory profiler
- real benchmark
- quadratic forms

  
forget about that:

MDP 1.1.0, 13.06.2005:

- Add Independent Slow Feature Analysis node

- Flow to handle acyclic graphs of nodes

- GUI with wxPython

