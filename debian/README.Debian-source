README on source packaging of mdp:
----------------------------------

Since 2.6, upstream tarballs are packaged directly from git
repository, no dh_wraporig is used any longer.

Upstream git repository at sourceforge could be added to
debcheckout'ed Debian packaging with something like

git remote add github git@github.com:mdp-toolkit/mdp-toolkit.git
git fetch github

